{
  "name": "client-app",
  "version": "1.0.0",
  "description": "Publisher para a criação do pedido",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "amqplib": "^0.10.3",
    "dotenv": "^16.5.0",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.1",
    "rimraf": "^5.0.1",
    "typescript": "^5.2.2",
    "@types/uuid": "^9.0.2"
  }
}

{
  "name": "common",
  "version": "1.0.0",
  "description": "Código compartilhado entre os microserviços",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "amqplib": "^0.10.3",
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "@types/amqplib": "^0.10.1",
    "rimraf": "^5.0.1",
    "typescript": "^5.2.2"
  }
}
